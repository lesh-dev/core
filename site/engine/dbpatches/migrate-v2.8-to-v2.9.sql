create table submission(
    submission_id integer primary key autoincrement,
    mail text,
    attachment text,
    fileexchange text,
    submission_timestamp text,
    sender text,
    contest_year text
);
