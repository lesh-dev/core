<?php
    $include_file_name = $SETTINGS["datadir"]."cms/pages/".@$pageid."/".@$argv[1];
    if (file_exists($include_file_name))
        include($include_file_name);
    else
        xcms_log(XLOG_ERROR, "Include file '$include_file_name' does not exist, ignored");
?>