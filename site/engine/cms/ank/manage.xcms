<?php
    include(translate("<! cms/ank/func !>"));
    if (@$_GET["act"]=="editAnk")
    {
        $preface = "ref=ladmin&amp;act=editAnk&amp;page=$pageid";
        include(translate("<! cms/ank/edit-ank !>"));
    }
    // TODO: мы уверены, что надо показывать список анкет
    // под редактором анкеты? Пока выглядит как фича
    include(translate("<! cms/ank/view-ank !>"));
?>