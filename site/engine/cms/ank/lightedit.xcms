<?php
    // Есть основательное подозрение, что этот тип content-а не используется по-существу
    // Выпиливать пока не надо, но нужно иметь это ввиду.
    if(@$_GET["vtype"]=="")
    {
        include(translate("<! var editfile {$SETTINGS["datadir"]}cms/pages/$pageid/content !><! edit/file xz -h !>"));
    }
?>
<?php
    $editfile = "$content_dir/cms/pages/$pageid/tags";
?>


