<?php
    // Есть подозрение, что этот тип content-а не используется по-существу
    // Выпиливать пока не надо, но нужно иметь это ввиду.
    $source = xcms_get_page_content_name($pageid);
    include(translate("<! var editfile $source !><! edit/file xz -h !>"));
    $editfile = "$content_dir/cms/pages/$pageid/tags";
?>