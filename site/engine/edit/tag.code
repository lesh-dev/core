<?php
    /*
        Seems to be rewritten soon [mvel@]
        This code invokes editlist_form to use new KV storage
    */
    $pathv = @$argv[1];
    if(!$pathv) $pathv = "edit-file";
    $ename = $editfile;
    if(!$ename) $ename = @$_POST[$pathv];
    if(!$ename) $ename = @$_GET[$pathv];
    if(!$ename)
        echo "<h4>List editor: illegal filename given</h4>";
    else
        @editlist_form($ename, "", @$argv[1], @$argv[2]);
?>
