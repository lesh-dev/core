<?
  $p_id = $pageid;
  $n = strlen($p_id);
  $count=0;
  for($i=0; $i<$n;$i++)
  {
    if($p_id[$i]=="/") $count++;
    if($count==2) {$p_id = substr($p_id,0,$i); break;}
  } 
  echo @file_get_contents("{$SETTINGS["datadir"]}cms/pages/$p_id/applymenu");
?>
